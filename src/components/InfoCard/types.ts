type InfoCardProps = {
  icon: JSX.Element;
  headline: string;
  body: string | undefined;
  project: Project | undefined;
  horizontalSlide: boolean;
  delay: number;
  time: number;
};

export type { InfoCardProps };
